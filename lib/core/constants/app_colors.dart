import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const Color primary = Color(0xFFFFD700); // Gold/Yellow accent
  static const Color secondary = Color(0xFF2C2C2C); // Darker gray for cards
  
  // Background colors
  static const Color background = Color(0xFF121212); // True dark background
  static const Color surface = Color(0xFF1E1E1E); // Elevated surfaces
  
  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFAAAAAA);
  
  // Status colors
  static const Color success = Color(0xFF4CAF50); // Income / Gets money
  static const Color error = Color(0xFFE53935);   // Expense / Owes money
  static const Color warning = Color(0xFFFFC107);
}
